use_frameworks!
 
target ‘Parking System’ do
    # Should only use this with projects 
    # that must have a minimum deployment 
    # target of iOS 8 
    platform :ios, '8.0' # (or '9.0' or '10.0')
    pod "PubNub", "~> 4"
    pod 'Alamofire'
    pod 'SwiftyJSON'
    pod 'SVProgressHUD'
end
